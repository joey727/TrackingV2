# TrackingV2

## Overview
`app.py` is the main script for the TrackingV2 application. This application is designed to track movement and get exact coordinates of the International Space Station as its in orbit

## Features
- Active tracking
- Google map cordinates pinpoint
- Refresh time flexibility 

## Requirements
Make sure you have the following installed:
- Python 3.x

## Installation
1. Clone the repository:
    ```bash
    git clone https://github.com/your-repo/TrackingV2.git
    ```
2. Navigate to the project directory:
    ```bash
    cd TrackingV2
    ```
3. Install dependencies:
    ```bash
    make sure you have requests module installed 
    ```

## Usage
Run the application in terminal 